# true -> usa el mock adapter en memoria (sin backend real)
NEXT_PUBLIC_USE_MOCK=true

# URL base del backend real. Solo se usa cuando NEXT_PUBLIC_USE_MOCK != "true"
NEXT_PUBLIC_API_URL=http://localhost:3001
